#include "main.h"

/**
 * print_numbers - prints the numbers, 0 through 9
 *
 * Return: 0-9 followed by new line
 */
void print_numbers(void)
{
	int x = 0;

	for (x = 0; x <= 9; x++)
		_putchar(c + '0');
	_putchar('\n');
}
