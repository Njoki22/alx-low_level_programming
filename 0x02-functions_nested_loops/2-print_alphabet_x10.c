#include "main.h"

/**
 * print_alphabet_x10:  function that prints 10 times the alphabet
 *
 * return: x10 a-z
 */
void print_alphabet_x10(void)
{
	char c;
	int x = 0
	{
		for (c = 'a'; c <= 'z'; c++)
			_putchar(c);
	}
	{
		for (x = 0; x <= 10; x++)
			_putchar(x);
	}
	putchar('\n');
	return (0);
}
