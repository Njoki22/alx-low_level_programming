#include "main.h"

/**
 * int _islower (int c) -  function that checks for lowercase character
 *
 * Return: 1 if c is lowercase otherwise return: 0
 */
int _islower(int c)
{
	int c;
	{
		if (int c = 'A' && int c <= 'Z')

			_putchar(int c);
	}
	putchar('\n');
	return (0);
	{
		else (int c = 'a' && int c <= 'z')

			putchar(int c is lowercase);
	}
	putchar('\n');
	return (0);
}
