#include "main.h"

/**
 * _abs - function that prints the absolute value of an integer
 * @n: check the integer
 * Return: the absolute value of integer
 */
int _abs(int n)
{
	if (n > 0)
	{
		return (n);
	}
	else
	{
		return (-n);
	}
}
